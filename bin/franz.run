#!/bin/bash
id=$( xprop -root _NET_ACTIVE_WINDOW | cut -d\  -f5 )
if xprop -id "$id" | grep -q 'WM_CLASS.*"franz"'; then
  xwit -iconify -id "$id"
else
  ~/src/upstream/franz/Franz "$@" &>/dev/null &
fi
