### general settings
set -g utf8 on
set -g status-utf8 on
set -g default-terminal 'screen-256color'
set -g history-limit 10000
set -g escape-time 0
set -g base-index 1
set -g renumber-windows on
set-window-option -g pane-base-index 1
set-window-option -g monitor-activity on

# disable italics
set -ga terminal-overrides ',*:sitm@,ritm@'

# desktop integration
set -g set-titles on
set -g set-titles-string '#{pane_current_command}: #{pane_title}'
set -g set-clipboard on

# mouse support
set -g mode-mouse on
set -g mouse-select-window on
set -g mouse-select-pane on
set -g mouse-resize-pane on

### keybindings {{{

set -g status-keys emacs
set -g mode-keys vi

# use C-` as the prefix key
unbind-key C-b
set -g prefix C-@
bind-key ` send-prefix
if '[ -n "$SSH_CONNECTION" -o -n "$CYGWIN" ]' 'set -g prefix2 C-g'
if '[ -n "$SSH_CONNECTION" -o -n "$CYGWIN" ]' 'bind-key g send-prefix -2'

# align windows vertically
bind-key = select-layout even-vertical

# reload the tmux configuration
bind-key R source-file ~/.tmux.conf

# zoom current pane
bind-key + resize-pane -Z

# switch between panes
bind-key    h run 'tmux select-pane -L || exit 0'
bind-key    j run 'tmux select-pane -D || exit 0'
bind-key    k run 'tmux select-pane -U || exit 0'
bind-key    l run 'tmux select-pane -R || exit 0'
bind-key    \ select-window -l
bind-key  C-h run 'tmux select-pane -L || exit 0'
bind-key  C-j run 'tmux select-pane -D || exit 0'
bind-key  C-k run 'tmux select-pane -U || exit 0'
bind-key  C-l run 'tmux select-pane -R || exit 0'
bind-key  C-\ select-window -l

# resize panes
bind-key -r H resize-pane -L 3
bind-key -r J resize-pane -D 3
bind-key -r K resize-pane -U 3
bind-key -r L resize-pane -R 3

# switch between windows
bind-key Space next-window

# swap windows
bind-key -r < swap-window -t -1
bind-key -r > swap-window -t +1

# scroll with PageUp/Down
bind-key -n S-PPage copy-mode -u
bind-key -t vi-copy S-NPage page-down

# copy mode bindings
bind-key -t vi-copy Home start-of-line
bind-key -t vi-copy End end-of-line
bind-key -t vi-copy ] cancel
bind-key -t vi-copy v begin-selection
unbind-key -t vi-copy C-j

# start new window in current directory (also requires PROMPT_COMMAND)
bind-key c new-window
bind-key C run 'tmux new-window -c "$(tmux show-environment $(echo "TMUXPWD_#D" | tr -d %) | cut -d= -f2- | grep . || echo "$PWD")"'

bind-key s run 'tmux split-window "cd \"$(tmux show-environment $(echo "TMUXPWD_#D" | tr -d %) | cut -d= -f2- | grep . || echo "$PWD")\"; exec bash --login"'
bind-key v run 'tmux split-window -h "cd \"$(tmux show-environment $(echo "TMUXPWD_#D" | tr -d %) | cut -d= -f2- | grep . || echo "$PWD")\"; exec bash --login"'

# move existing windows into new pane in current window
bind-key S choose-window 'join-pane -v -s "%%"'
bind-key V choose-window 'join-pane -h -s "%%"'

### }}}

### plugins {{{

run ~/.tmux/yank/yank.tmux
run ~/.tmux/open/open.tmux

# copycat
# set-option -g @copycat_search_C-f '(^|^\.|[[:space:]]|[[:space:]]\.|[[:space:]]\.\.|^\.\.)[[:alnum:]~_]*/[][[:alnum:]_.#$%&+=/@-]*(:[0-9]+)?'
# set-option -g @copycat_search_C-u '(https?://|[[:alnum:]]+@[-_\.[:alnum:]]+:|ftp://)[[:alnum:]?=%/_.:,;~@!#$&()*+-]*'
# set-option -g @copycat_search_M-h '\b([0-9a-f]{7}|[0-9a-f]{32}|[0-9a-f]{40}|[0-9a-f]{64})\b'

run ~/.tmux/copycat/copycat.tmux

bind-key -n M-/ run '~/.tmux/copycat/scripts/copycat_search.sh'
bind-key -n M-p run '~/.tmux/copycat/scripts/copycat_mode_start.sh "$( tmux show-options -gv @copycat_search_C-f )"' # files
bind-key -n M-h run '~/.tmux/copycat/scripts/copycat_mode_start.sh "$( tmux show-options -gv @copycat_search_M-h )"' # hashes
bind-key -n M-u run '~/.tmux/copycat/scripts/copycat_mode_start.sh "$( tmux show-options -gv @copycat_search_C-u )"' # URLs
bind-key -n M-i run '~/.tmux/copycat/scripts/copycat_mode_start.sh "$( tmux show-options -gv @copycat_search_M-i )"' # IPs
bind-key -n M-g run '~/.tmux/copycat/scripts/copycat_git_special.sh "#{pane_current_path}"' # git status

bind-key -n M-e run '~/.tmux/copycat/scripts/copycat_mode_start.sh "[-_\.[:alnum:]]+@[-_\.[:alnum:]]+"'                    # email addresses
bind-key -n M-t run '~/.tmux/copycat/scripts/copycat_mode_start.sh "\b(bugs?|chores?|features?|fix(es)?)/[-[:alnum:]]+\b"' # branches
bind-key -n M-l run '~/.tmux/copycat/scripts/copycat_mode_start.sh ".*"; ~/.tmux/copycat/scripts/copycat_jump.sh next'     # lines

# powerline
set-environment -g POWERLINE_CONFIG_COMMAND ~/.tmux/powerline/scripts/powerline-config
source ~/.tmux/powerline/powerline/bindings/tmux/powerline.conf

if '[ -n "$SSH_CONNECTION" ]' 'set-environment -g NORMAL_PROMPT " #H "' 'set-environment -g NORMAL_PROMPT ""'
if '[ -n "$SSH_CONNECTION" ]' 'set-environment -g PREFIX_PROMPT " #H "' 'set-environment -g PREFIX_PROMPT ""'

set-option -g status-left '#{?client_prefix,#[fg=colour254]#[bg=colour24]#[bold]#[noitalics]#[nounderscore],#[fg=colour23]#[bg=colour159]#[bold]#[noitalics]#[nounderscore]}#{?client_prefix,#(echo "\$PREFIX_PROMPT"),#(echo "\$NORMAL_PROMPT")}#{?client_prefix,#[fg=colour24],#[fg=colour159]}#[bg=colour236]#[nobold]#(env \$POWERLINE_COMMAND \$POWERLINE_COMMAND_ARGS tmux left)'
set-option -g status-right '#[fg=colour248]#[fg=black,bg=colour248,bold] %H:%M '

# vim-tmux-navigator
bind-key -n C-h if-shell 'expr "#{pane_title}" : ".* ◉$"' 'send-keys C-h'  'run "tmux select-pane -L || exit 0"'
bind-key -n C-j if-shell 'expr "#{pane_title}" : ".* ◉$"' 'send-keys C-j'  'run "tmux select-pane -D || exit 0"'
bind-key -n C-k if-shell 'expr "#{pane_title}" : ".* ◉$"' 'send-keys C-k'  'run "tmux select-pane -U || exit 0"'
bind-key -n C-l if-shell 'expr "#{pane_title}" : ".* ◉$"' 'send-keys C-l'  'run "tmux select-pane -R || exit 0"'
bind-key -n C-\ if-shell 'expr "#{pane_title}" : ".* ◉$"' 'send-keys C-\\' 'select-window -l'

### }}}
